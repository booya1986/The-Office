{
  "name": "@pixel-office/cloud-service",
  "version": "0.1.0",
  "description": "Backend for Pixel Office Simulator - connects visual office to Claude API",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "fastify": "^4.26.0",
    "@fastify/cors": "^8.5.0",
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}
